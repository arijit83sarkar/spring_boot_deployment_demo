version: "3.8"

services:
  app:
    build: .
    container_name: product-app
    ports:
      - "8080:8080"
    environment:
      # Optional: if you want to override application.properties
      SPRING_PROFILES_ACTIVE: default
